# SPEC — merged into README.md

> **This file is retired as a working draft.** Its content was written directly into the canonical **`README.md`** (repo root) during M2 (intent pass, 2026-06-07). Per the documentation protocol (`00_INDEX.md`), the **README is the single source of truth**; `architecture.html` is its visual companion.

Do not maintain spec content here. Edit the README.

**Remaining M2 work (truth pass):** after M4b lands, reconcile the README's **[current]** vs **[target]** flags — delete the flags as each becomes true, so the README describes exactly what runs.
